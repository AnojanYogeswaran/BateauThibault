import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Bateau_Thibault',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
